import {describe,expect,it} from 'vitest'; import {defaults,migrate} from './store';describe('almacenamiento',()=>{it('recupera datos corruptos',()=>expect(migrate(null)).toEqual(defaults()));it('migra y valida',()=>{const x=migrate({version:1,recent:['uno',2],dailyStats:{played:'mal'}});expect(x.version).toBe(2);expect(x.recent).toEqual(['uno']);expect(x.dailyStats.played).toBe(0)})});

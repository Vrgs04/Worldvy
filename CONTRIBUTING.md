# Contribuir

Usa Node 22+ y pnpm 10. Ejecuta `pnpm install`, crea una rama, añade pruebas y termina con `pnpm check`. Las modificaciones del vocabulario manual deben justificar cada entrada en `data/manual-*.txt`. No incluyas secretos.

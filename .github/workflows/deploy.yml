name: Validar y desplegar
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: read
  deployments: write
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 10.15.0 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - name: Garantizar proyecto de Cloudflare Pages
        if: github.event_name == 'push'
        run: |
          if ! pnpm exec wrangler pages project list --json | jq -e --arg name "$CLOUDFLARE_PROJECT_NAME" 'map(select(.name == $name)) | length > 0'; then
            pnpm exec wrangler pages project create "$CLOUDFLARE_PROJECT_NAME" --production-branch main
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_PROJECT_NAME: ${{ vars.CLOUDFLARE_PROJECT_NAME }}
      - name: Desplegar Cloudflare Pages
        if: github.event_name == 'push'
        run: pnpm exec wrangler pages deploy dist --project-name "$CLOUDFLARE_PROJECT_NAME" --branch main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_PROJECT_NAME: ${{ vars.CLOUDFLARE_PROJECT_NAME }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
